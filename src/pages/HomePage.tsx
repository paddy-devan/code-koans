import { PlaceholderPage } from "../components/PlaceholderPage";

export function HomePage() {
  return (
    <PlaceholderPage
      title="Home"
      description="This is the initial app shell. Later checkpoints will replace this placeholder with a proper landing page."
    />
  );
}
